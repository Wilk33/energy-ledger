name: Tests

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate Home Assistant app configuration
        uses: frenck/action-app-linter@v2
        with:
          path: "./energy-ledger"
      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"
      - name: Install runtime dependencies
        run: python -m pip install -r energy-ledger/rootfs/requirements.txt
      - name: Verify generated runtime copy
        run: |
          python tools/sync_runtime.py
          git diff --exit-code
      - name: Run unit tests
        run: python -m unittest discover -s tests -v
      - name: Compile Python sources
        run: python -m compileall -q energy_ledger energy-ledger/rootfs/usr/src/app/energy_ledger
